define([
    'underscore',
    'backbone'
], function (_, Backbone) {
    'use strict';

    var AuctionItem = Backbone.Model.extend({
        defaults: {
        }
    });

    return AuctionItem;
});